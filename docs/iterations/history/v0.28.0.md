# v0.28.0 内容数据平面与内容增量发布架构

状态：Engineering 实现中（未提交、未验收、未发布）。

父版本：v0.27.9 / `7374b75a7a611217cfa0199e2d81ec7964dc3386`

- implementation：ContentDataArtifact、CAS/changed-only、runtime reader、原子 active tuple、内容-only 临时 materialization、最小 receipt 和失败回退。
- record-only：current、design、候选归档、history、基线规则同步；不进入 ProductArtifact 运行输入。
- 内容事实仍由 ignored `.content-workspace` 与 ContentSet/Ops 独立管理；本版本不执行物理清理、迁移、transport 或 content publish。

## 当前状态

等待 `elon` 对 SA-00～SA-11 未提交 machine evidence 逐项复核；收到 `READY_FOR_COMMIT` 前不 commit/tag/build/preflight/transport/content publish。

