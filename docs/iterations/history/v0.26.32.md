# v0.26.32 任务注册表治理记录版本

父版本：`v0.26.31` / `3c609a78793c11b71023feee7b8bced0633d0b2a`

## Record-only 治理范围

- 原样记录既有 `docs/rules/task-registry.md` host 身份治理提交：`6cd931e2325d2ca295835efac16589acaafb965d`。
- 该治理事实已存在于父版本之后的 canonical HEAD；本版本将其纳入正式版本记录，不重复改写文件内容。
- `docs/design/v0.26.32 任务注册表治理记录版本方案.md` 与 `docs/iterations/current.md` 说明本版本仅为治理记录和版本身份收口，不进入产品功能、ContentSet 或发布实现范围。

## 版本边界与验证

- 更新 `VERSION.md`、`package.json`、`package-lock.json` 与本 history 文件；不改 UI、内容、schema、组件、视觉、ContentSet、SitePublication 或发布器。
- 通过 `npm run check`、`npm run release:prepare`、`npm run release:closeout-check`、exact `release:build`、`release:preflight` 与 `git diff --check` 后，以提交后的 exact HEAD/tag 生成 ProductArtifact。
- 本版本不执行 content publish、product transport 或 EdgeOne；产品/内容运行事实保持只读。
