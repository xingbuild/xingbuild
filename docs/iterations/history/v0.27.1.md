# v0.27.1 版本记录

父版本：`v0.27.0` / `2e4c72a1c5bfc04501b758b4c70b30d42d5ecdfe`

- 正式方案：`docs/design/v0.27.1 内容生命周期真实链路收口方案.md`。
- `V271-01`：真实 ContentSet Candidate prepare 生成变化 target 的 ContentRevision/ChangeSet ref，未变化 entry 复用原 identity/hash，并保存不可变 ChangeSet sidecar。
- `V271-02`：真实 SitePublication assembly/Coordinator 写入 reference-only durable record；SiteSnapshot、PublicationRun、assembled client 与 uploadRoot 只保留为运行时/sidecar引用。
- `V271-03`：inventory 对 SitePublication、receipt、lineage、recovery、lease 和未证明引用保守 keep/delete-never；dry-run 零写入。
- `V271-04`：纳入 `v0.27.0-correction` record receipt，明确候选已归档转换、DL/CL 已实现但未发布，不回写 v0.27.0 tag/history。

## 验证与发布边界

- 本版本不修改页面、内容事实、active ContentSet 或线上状态，不执行 content publish、product transport 或 EdgeOne。
- 通过 check、release prepare/closeout、exact HEAD build、preflight、生命周期/SitePublication 定向测试后交产品/架构验收。
