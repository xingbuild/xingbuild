# v0.28.5 — 内容 Authority 与产品 SiteSnapshot 解耦及 QA Browser 单会话

父版本：`v0.28.4` / `b23d76a567645b222605a3944611825a7441db00`

- ActiveContentDataTuple v2 只保存 ContentSet/CDA/object refs 与 content-only manifest hash；ProductArtifact 退出内容 active authority。
- v0.28.3 active tuple 保留原始 bytes，按 tuple、ContentSet、CDA、object refs 与持久化 approved manifest exact 证明后只读适配，未写旧 authority。
- SiteSnapshot v1 继续是唯一发布组合对象；产品发布保持 active tuple 不变，内容发布在 Coordinator public verification 后执行一次 CAS。
- QA runtime 以一个 browser batch/global lease 持有一个受控 Chrome，每个场景创建并串行关闭新的 BrowserContext，机器 evidence 记录资源计数与清理结果。
- 最终旧站稳定化补齐 active observations 集合/slug、Robotaxi approved media、诚实 loading 与真实 robots/sitemap；公网逐页通过后旧站冻结，不再继续功能迭代。
- 本记录只描述本版本 Engineering 变更；不产生 ApprovalRecord、commit/tag/build/preflight/transport/content publish/EdgeOne 事实。

状态：`candidate-self-qa-pending`
