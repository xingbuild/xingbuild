# v0.28.1 — Release Transaction 不可变批准与提交事务

父版本：v0.28.0 / `f7146bf654a9f6fd6467255746d6a07d9eec47c7`

## 本版本记录

- 建立 classification-only scope、staged-tree `CandidateIdentity`、单一 `ApprovalRecord`、client-only ProductArtifact 根 manifest 与现场 SideEffectBaseline。
- 接入三类 observer、I-01～I-08 invariant、atomic commit/tag 与可恢复的 annotated-tag authority；scope 不再保存 pathHash。
- `v0.28.0` 保持 `BLOCKED_LOCAL_ARTIFACT`、未发布且不可变；本版本不执行产品 transport、content publish、EdgeOne 或内容事实修改。

## 验证边界

- Engineering 先完成 exact staged tree 与 I-01～I-08 自 QA，生成 CandidateIdentity；`elon` 对精确候选生成 ApprovalRecord，随后才能执行 atomic commit/tag/build/preflight。最终 closure 只引用 Git、release.json、tag 和现场 protected diff，不复制完整 evidence。最终 commit/tag 由 Git 解析，不在本文件写自引用 hash。
