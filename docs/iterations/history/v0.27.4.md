# v0.27.4 — 内容存储治理证据门禁修复

父版本：`v0.27.3` / `2cb564c81834ac37e6b28e598113ebff00be91e0`

## 范围

- 修复 v0.27.3 evidence false-pass：AC74-01～AC74-14 统一由真实 acceptance reducer/validator 计算。
- 增加 exact ProductArtifact identity、annotated tag、full deterministic inventory、逐字节 SHA-256、resolved/external/unresolved reference graph、outputRoot durable/materialization 分离与零动作 post-action evidence。
- AC74-13 在未获生产发布授权时明确 `N/A`；本版本不执行物理删除、迁移、quarantine、content publish、product transport 或 EdgeOne。

## 证据与门禁

- 固定证据：`.content-workspace/qa/v0274-storage-governance/evidence.json`。
- `release:prepare`/closeout 使用同一 validator 的 pending 检查；exact `release:build` 生成最终 evidence，`release:preflight` 严格校验 identity 与无 FAIL。
- v0.27.3 保持 immutable，不回写旧 tag/history；既有 retained/environment failures 原样分层。
