# v0.26.28 — 本地内容工作台结构化正文、差异更新与产品化入口

父版本：`v0.26.27` / `e15fa851d2a34f891d254403b0f3559902ad4c93`

## 范围

- About 与经营观察长文正文支持 `content-rich-text-list-v1`；旧单字符串只在真正编辑为多段时升级，普通旧字段与 `responsive-text-slot-v1` 保持兼容。
- 重复 block/item ID 在 target read/authoring 前硬失败并返回可定位的 `CONTENT_TARGET_AMBIGUOUS_ID`，不再静默选择第一项。
- 保存响应记录 target、before/after hash、差异段数和实际 `consumerViews`；只刷新登记消费者，不触发全站 reload/build 或发布链路。
- 移除工作台关系线，新增固定 4317 的 `start-content-preview.command`，未知占用明确停止并不换端口。
- 按 Xing 授权，对 canonical ignored 内容仅完成三组重复 paragraph 身份修复：保留 heading ID，paragraph 使用 `operations-facts-results-body`、`analysis-and-feedback-body`、`direction-body`；不进入 active ContentSet 或产品提交。

## 边界

未修改公开页面 IA、产品视觉、ContentSet active、review/recovery/release、ProductArtifact、SitePublication、deployment 或 EdgeOne；未执行 content publish/product transport。

## 验证

定向覆盖 rich 编解码、真实段落渲染、重复 ID 零写入、TargetImpact 局部刷新、关系线移除、固定启动器静态门禁；固定 4317 Xing 实际使用证据与零写入快照随版本回传。
