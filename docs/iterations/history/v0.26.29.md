# v0.26.29 内容工作台预览源统一与目标级内容发布

父版本：`v0.26.28` / `7caf8eb66011302e04275cf928da4aa8644bdff4`

## 实现

- Home Candidate 唯一读取 `.content-workspace/content/home.json`，并让 runtime、`home:home` entry 与 Candidate 共享 normalized value、hash、sourcePath。
- 增加 canonical source 缺失、非法 JSON、source mapping mismatch、hash 不可重现和重复 Candidate entry 的写入前硬门禁。
- 支持 Home、Practice、Article、Profile 同批 Candidate 合并；未变化 active entry 保持 identity。
- Preview Runtime v2 继续只按 `TargetImpact.consumerViews` 局部刷新；预览和 Candidate 不写 active ContentSet、review、release、SitePublication 或 ProductArtifact。

## 验证与发布边界

- 通过 v0.26.29 定向 source/Candidate 回归及既有 content/preview 回归；保留历史 v0.26.28 authoring fixture drift 作为分层 retained failure。
- 版本完成 local commit、annotated tag、clean、exact ProductArtifact/preflight 后交产品/架构验收。
- 本版本未执行 product transport、content publish 或 EdgeOne；产品上线后由 `elon ops` 按独立内容流程处理已记录内容。

## Record-only 与治理范围

- `docs/iterations/candidates/XBUILD-PAGE-CONTENT-STRUCTURE-AUTHORING-001.md` 原样保留为 `DRAFT` record-only 记录；不进入 `current.md`、Engineering 实现、ProductArtifact 或发布范围。
- 本版本同时收口最小 task 状态、交接回执和候选/版本范围分类规则；这些治理文档不新增产品功能，不改变内容事实或发布语义。
