# v0.28.4 — 公网内容 Runtime Ready 与同一 Deployment 恢复

父版本：v0.28.3 / `85e8c3d080f998449a4fefb0c8429b1e27beb36e`

- `RuntimeAcceptanceSpec` 是同一 ContentPublicationIntent/SiteSnapshot 的只读验收投影，不是第二内容 authority。
- browser verifier 只在 shellReady、公网身份 exact、runtimeReady exact 三类事实同时成立时返回 verified；timeout、abort、错误内容和 data-object failure 保留结构化 evidence。
- `RuntimeAcceptanceSpec` 在每个 canonical verify/recovery 入口都从 approved `contentManifest` 重新派生并完整 exact compare；同 identity 的 fallback 文案不能自证通过。
- existing-publication recovery 只接受明确授权的 failed/recoverable post-transport incident，持有固定 site lease；失败和成功都同步记录 SitePublication、PublicationRun 的 recovery attempt/history，复用唯一成功 deployment，transportCalls=0、deploymentCount=1；最终 active tuple 仍只由 Coordinator CAS 写入。
- 既有 v0.28.3 `dpgr0trnxfcv` 事故是唯一缺失 spec 的兼容边界：严格匹配已登记 ProductArtifact/SiteSnapshot/ContentSet/ContentData/active tuple/target/deployment 身份，从旧记录已持久化且文件 exact 的 approved `contentManifest` 派生 spec，并把完整 spec、hash 与来源写入每个 recovery attempt；其他缺失 spec 的 data-plane publication hard fail。
- 正式隔离链复制真实 `state=failed`、`failure.phase=verified`、`runtimeAcceptanceSpec=null` 的 SitePublication/PublicationRun/deployment 字节，经授权 recovery 成功，零 transport、同一 deployment、双记录失败/成功 history 与 active tuple CAS 均有 machine evidence。

Candidate 阶段仅形成未提交 Engineering evidence，不生成 ApprovalRecord、commit/tag、final build/preflight 或发布事实。
