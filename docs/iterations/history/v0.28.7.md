# v0.28.7 — Legacy Active Tuple 最终只读发布适配

- 从 v1 tuple 的不可变历史 ProductArtifact exact 重建原 content manifest，拒绝 provenance/hash 漂移。
- 当前 ProductArtifact 只读复用既有 active ContentSet/CDA/tuple；v2 content-only authority 路径不变。
- 本地正向组装已使用真实 v0.28.6 ProductArtifact 与当前 v1 authority，通过后进入 exact Candidate/Approval/commit/tag/build/preflight 与单次产品发布。
