# v0.27.3 — 内容派生存储保留与清理闭环

父版本：`v0.27.2` / `ae0befadc177e049380795c948109ddd31d7c13d`

## 版本范围

- 实现 SG-01～SG-07：protected-root、可重跑 metadata inventory/reference graph、namespace CAS、staging/upload/outputRoot 分离、retention 分类、quarantine/restore 与显式 cleanup gate。
- 保留 v0.27.0～v0.27.2 已完成的 ContentRevision、ChangeSet、changed-only 与 durable SitePublication；本版本不重复实现生命周期。
- 只生成 ignored machine evidence；不改 UI、内容事实、active ContentSet、review、media、SitePublication 或线上状态。

## 验证

- `npm run content:storage:check` 生成 `.content-workspace/qa/v0273-storage-governance/evidence.json`：inventory/reference graph、dry-run zero-write 与 AC-01～AC-14 矩阵；AC-13 因生产发布未授权记录 N/A。
- 通过 `npm run check`、`npm run release:prepare`、存储治理定向/回归测试、`npm run release:closeout-check`、exact `release:build`、`release:preflight` 与 `git diff --check` 后交产品/架构验收。
- 不执行物理删除、content publish、product transport 或 EdgeOne；CL-05 保留独立授权门禁。

## Record-only

- 本版本仅保留正式方案、归档候选和机器证据；任何后续清理必须经过独立授权、保留窗口与恢复验证，不由本版本自动执行。
