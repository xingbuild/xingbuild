# v0.27.0 版本记录

父版本：`v0.26.32` / `50643e6ea1edac080759e292d30b447aff64b293`

## 范围

- 正式方案：`docs/design/v0.27.0 内容生命周期、变更复用与派生物治理方案.md`。
- 实现 `DL-01`～`DL-04`：ContentRevision、变化 target 的 ChangeSet/ref 复用、SitePublication 引用收敛、确定性 SiteSnapshot 与失败可恢复的只读模型。
- 实现 `CL-01`～`CL-04`：可重跑 inventory、跨对象引用图、保留分类与零写入 dry-run。
- `CL-05`（保留窗口与物理清理）明确不在本版本范围，需 Xing 另行授权。

## Record-only 记录

- `docs/iterations/history/candidates/XBUILD-CONTENT-LIFECYCLE-GOVERNANCE-001.md` 原样归档并纳入本版本记录；候选仍为 `DRAFT`，不进入 `current.md`、Engineering、ProductArtifact 或发布实现范围。
- `docs/rules/task-registry.md` 的活动 task `hostId=local` 核验作为 record-only 治理记录纳入；不改变职责、threadId 或线上产品事实。

## 不变边界

- 不改页面 IA、组件、视觉、schema、正文/审核事实、active ContentSet、SitePublication 线上状态或发布器；不运行 content publish、product transport、EdgeOne。
- inventory 与 dry-run 只读，不删除、迁移或覆盖未知引用；完整 client 不写入长期 SitePublication 记录。

## 验证

- 生命周期定向测试覆盖 revision retention、changed-only ChangeSet/ref、deterministic snapshot、durable SitePublication、交叉引用 inventory 与 zero-write dry-run。
- 版本门禁以提交后 exact HEAD/tag 的 ProductArtifact 与 preflight 结果为准。
