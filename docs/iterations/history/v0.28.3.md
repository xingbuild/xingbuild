# v0.28.3 — 内容数据发布事务与首次 Active Tuple 切换

父版本：`v0.28.2` / `3a615ce0ff712be339cef0c4fc8387ae0d0bd779`

- 建立唯一 `ContentPublicationIntent`，把 ProductArtifact、ContentSet、ContentDataArtifact、candidate active tuple、SiteSnapshot 与 PublicationRun 绑定为一次不可变内容发布输入。
- 完成 legacy active ContentSet 的只读 baseline 重建与 1 changed / 37 reused 的首页增量；内容-only materialization 复用 exact ProductArtifact client，并拒绝 source bundle、客户端字节或对象 hash 漂移。
- Coordinator 在真实隔离 HTTP/浏览器 public proof 后，以 CAS 原子写入 `content-data-active.json`；旧 `active.json` 保持审计字节，失败、恢复、重复和 CAS 冲突不污染 active authority。
- 同步规则、版本、scope、稳定 QA route 与 machine evidence；本版本 Engineering 阶段不生成 ApprovalRecord、不 commit/tag/final build/preflight/transport/content publish/EdgeOne。

## 验证与发布边界

- `CP-01`～`CP-11` 通过同一 intent/tuple-aware production modules、fault matrix 与 exact local HTTP/browser positive chain 自 QA；`CP-12` 只交付一个未提交 CandidateIdentity。
- `ContentPublicationIntent` 与 `content-publication-evidence.json` 位于 ignored QA 工作区，未改变 canonical 内容、旧 active pointer、旧 ProductArtifact 或线上事实。
