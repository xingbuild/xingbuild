# v0.26.21 — 精准内容预览与局部刷新闭环

父版本：`v0.26.20` / `3ee065faa1ef3c847e221743052e686cd2e5525b`

## 实现

- 以注册 `projectionRoutes` 和既有 `PageDefinition.contentRefs` 解析 TargetImpact，修正 `products.robotaxi.intro` 的真实消费者为 `/` 与 `/products`。
- ContentPreviewSession 保存 consumer routes/views、source/value hash 与 revision；工作台展示所有受影响 route 的 Web1280/Mobile390 frame。
- Vite content-preview dev server 使用 `xingbuild:content-target-update` 自定义事件，仅刷新选中 target 的消费者 frame；非法 JSON/slot 保留上次有效页面，未选中字段明确标记 `outside-selected-target`。

## 边界

- 仅本地 dev-only 预览；不写 active ContentSet、review/recovery/release/SitePublication、ProductArtifact 或线上状态。
- 不修改正式网站 UI/IA/视觉、内容值、审核/media 事实或发布链路；不执行 content publish、product transport、EdgeOne。

## 验证

- TargetImpact、route/contentRef、局部 reducer、invalid→修复、source hash 和全站 reload 禁止专项通过；保留 v0.26.20 预览回归。
