# v0.27.0 记录更正

v0.27.0 的 `XBUILD-CONTENT-LIFECYCLE-GOVERNANCE-001` 已从活动候选归档转换；`DL-01`～`DL-04`、`CL-01`～`CL-04` 已形成代码模型与定向测试，并完成 local tag，但未 product transport、未 content publish。

原 `history/v0.27.0.md` 中“候选仍为 DRAFT、未进入 Engineering”的文字与实际归档/工程事实不一致。该事实更正只作为 record receipt 保存，不回写 v0.27.0 tag。
