# v0.27.7 Scope classifier 路径身份修复方案

## 定位

v0.27.6 的 commit/tag 已完成，但 `release:build` 因非 NUL Git 路径解析误判中文文件缺失。修复证据读取，不回写旧 tag。

## 必须实现

- `V277-01`：`commitPaths` 使用 NUL-safe Git 输出读取 committed paths，禁止依赖 `core.quotePath` 的转义文本。
- `V277-02`：路径比较统一使用 Git 原始路径；支持中文、空格、引号、删除和重命名的 from/to 身份。
- `V277-03`：post-commit scope validator、release-build、preflight、unified-publish 继续共用同一 classifier；不改变 v0.27.6 的分类、scopeDigest、ProductArtifact 或 Content/Ops 边界。

## 验收 checklist

- `C277-01`：v0.27.6 committed scope 含中文路径时，validator 正确识别已提交路径。
- `C277-02`：中文、空格、引号、删除、重命名路径的正向与负向测试通过。
- `C277-03`：同一 committedHead 的 firstParent、scopeDigest 和声明路径校验保持正确。
- `C277-04`：exact release:build 与 release:preflight 能继续读取 scope，不产生 tracked dirty。
- `C277-05`：既有 v0.27.6 classifier、ContentSet、SitePublication、.content-workspace 与旧 tag 不变。

## 交付门禁

Engineering 先按本 checklist 自 QA 并回传未提交证据；`elon` 复核后回传 `READY_FOR_COMMIT`。此前不得 commit/tag/build/preflight/transport/content publish。
